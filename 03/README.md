`docker-compose up --build`
`docker-compose up`
`docker-compose up -d --build`
`docker-compose down`
`docker-compose ps`

docker build -t visits .
docker run -it visits sh
docker run visits
docker run -it visits
